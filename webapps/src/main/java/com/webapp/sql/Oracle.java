package com.webapp.sql;

public class Oracle {

	// top
	// 1 select name from user where rownum < 3
	// 2 select rownum, rowid from dept;
	// 3 select rownum || '-' || rowid from dual;
	// 4 select nvl(rownum, 'not rownum') from dual;

}
