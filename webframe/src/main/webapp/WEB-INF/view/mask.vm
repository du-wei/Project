<!DOCTYPE html>
<html lang="en">
<head>
#parse("/common/var.vm")
<title>$!{title}</title>
#parse("/common/head_jquery.vm")

<style>
body{
	background:url(/images/1.jpg) top / cover;
}

canvas{
	position:absolute;
	left:0;
	top:0
}

</style>
<script>
(function () {
  window.navigator.userAgent.match(/Macintosh/);
  $(k);
  function k() {
    $('form').each(function () {
      0 === $(window).height() / 2 ? window.setTimeout(function () {
        k()
      }, 100)  : $(this).css('margin-top', - $(this).outerHeight() / 2)
    })
  };
  function q(a, b) {
    this.i = (this.h = 'PUBLIC' !== a) ? a : '*';
    this.l = b
  }
  q.prototype.init = function (a) {
    window.addEventListener('message', function (b) {
      b = b.data;
      b.isCanvaApi && (a[b.type] || $.noop) (b)
    }, !1);
    r(this, {
      type: 'Init',
      authenticated: this.h
    })
  };
  function r(a, b) {
    b.isCanvaApi = !0;
    a.l.postMessage(b, a.i)
  };
  (function () {
    function a() {
      $('body').mousemove(function (a) {
        m = a.clientX;
        p = a.clientY;
        t = Date.now();
        n || l()
      });
      $(window).on('blur mouseout', function () {
        p = m = null
      }).on('resize', function () {
        d && d.parentNode && d.parentNode.removeChild(d);
        b()
      });
      b()
    }
    function b() {
      var a = null == g ? !0 : !1;
      d = document.createElement('canvas');
      d.width = $(window).width();
      d.height = $(window).height();
      $('body').append(d);
      g = document.createElement('canvas');
      g.width = $(window).width();
      g.height = $(window).height();
      if (d.getContext && d.getContext('2d')) {
        h =
        d.getContext('2d');
        e = g.getContext('2d');
        e.lineCap = 'round';
        e.shadowColor = '#000000';
        e.shadowBlur = 30;
        c = new Image;
        var b = $('body').css('background-image');
        b && ($(c).one('load', function () {
          a && l()
        }),
		b = b.replace(/url\((.*)\)/, '$1').replace(/["']/gi, '').replace(/\.jpg/, '_color.jpg'), 

		$(c).attr('src', b), b = $('<div style="position:absolute;height:0;width:0;overflow:hidden;"></div>'), $('body').append(b), b.append(c))
      }
    }
    function l() {
      var a,
      b = Date.now();
      n = b > t + 500 ? !1 : !0;
      m && n && f.splice(0, 0, {
        time: b,
        x: m,
        y: p
      });
      for (a = 0; a < f.length; ) 1000 < b - f[a].time ? f.splice(a, f.length)  : a++;
      0 < f.length && window.j(l);
      e.clearRect(0, 0, g.width, g.height);
      for (a = 1; a < f.length; a++) {
        var v = Math.sqrt(Math.pow(f[a].x - f[a - 1].x, 2) + Math.pow(f[a].y - f[a - 1].y, 2));
        e.strokeStyle = 'rgba(0,0,0,' + Math.max(1 - (b - f[a].time) / 1000, 0) + ')';
        e.lineWidth = 25 + 75 * Math.max(1 - v / 50, 0);
        e.beginPath();
        e.moveTo(f[a - 1].x, f[a - 1].y);
        e.lineTo(f[a].x, f[a].y);
        e.stroke()
      }
      a = d.width;
      b = d.width / c.naturalWidth * c.naturalHeight;
      b < d.height && (b = d.height, a = d.height / c.naturalHeight * c.naturalWidth);
      h.drawImage(c, 0, 0, a, b);
      h.globalCompositeOperation = 'destination-in';
      h.drawImage(g, 0, 0);
      h.globalCompositeOperation = 'source-over'
    }
    var d,
    g,
    h,
    e,
    c,
    m = null,
    p = null,
    f = [
    ],
    t = 0,
    n = !0;
    'createTouch' in document || $(a);
    window.j = function () {
      return window.requestAnimationFrame || window.webkitRequestAnimationFrame || window.mozRequestAnimationFrame || window.oRequestAnimationFrame || window.msRequestAnimationFrame || function (a) {
        window.setTimeout(a, 1000 / 60)
      }
    }()
  }) ();
  $(function () {
    var a = null;
    $('menu .dropdown').on('mouseover', function () {
      if (0 >= $(this).parents('#movedMenus').length) {
        var b = $(this);
        null != a && clearTimeout(a);
        a = setTimeout(function () {
          b.removeClass('hoverOff').addClass('hover')
        }, 0);
        b.on('mouseout', function () {
          null != a && clearTimeout(a);
          a = setTimeout(function () {
            b.off('mouseout').removeClass('hover').addClass('hoverOff')
          }, 500)
        })
      }
    })
  });
}) ();

</script>
</head>
<body> 
	
</body>
</html>